{"items":[{"tags":["python","json","rest","server","web-development-server"],"owner":{"account_id":30109810,"reputation":16,"user_id":23074625,"user_type":"registered","profile_image":"https://www.gravatar.com/avatar/081f45a8f56b9bead4af793a0b0ee676?s=256&d=identicon&r=PG&f=y&so-version=2","display_name":"Yogender","link":"https://stackoverflow.com/users/23074625/yogender"},"is_answered":true,"view_count":7,"answer_count":2,"score":0,"last_activity_date":1777811129,"creation_date":1777810510,"question_id":79935417,"content_license":"CC BY-SA 4.0","link":"https://stackoverflow.com/questions/79935417/how-does-basehttprequesthandler-send-http-headers-and-json-response-body-in-pyth","title":"How does BaseHTTPRequestHandler send HTTP headers and JSON response body in Python?","body":"<p>I am learning backend development and REST APIs. I created a very small HTTP server in Python using <code>BaseHTTPRequestHandler</code>.</p>\n<p>Here is my code:</p>\n<pre><code>from http.server import BaseHTTPRequestHandler, HTTPServer\nimport json\n\nclass Handler(BaseHTTPRequestHandler):\n    def do_GET(self):\n        print(&quot;---request received&quot;)\n        print(&quot;path:&quot;, self.path)\n        print(&quot;headers:&quot;, self.headers)\n\n        if self.path == &quot;/&quot;:\n            body = {\n                &quot;message&quot;: &quot;Hello bro, this is your HTTP response&quot;\n            }\n\n            response_body = json.dumps(body).encode(&quot;utf-8&quot;)\n\n            self.send_response(200)\n            self.send_header(&quot;Content-Type&quot;, &quot;application/json&quot;)\n            self.send_header(&quot;Content-Length&quot;, str(len(response_body)))\n            self.end_headers()\n\n            self.wfile.write(response_body)\n\n        else:\n            body = {\n                &quot;error&quot;: &quot;Route not found&quot;\n            }\n\n            response_body = json.dumps(body).encode(&quot;utf-8&quot;)\n\n            self.send_response(404)\n            self.send_header(&quot;Content-Type&quot;, &quot;application/json&quot;)\n            self.send_header(&quot;Content-Length&quot;, str(len(response_body)))\n            self.end_headers()\n\n            self.wfile.write(response_body)\n\nserver = HTTPServer((&quot;localhost&quot;, 8000), Handler)\nprint(&quot;server running on localhost 8000&quot;)\nserver.serve_forever()\n</code></pre>\n<p>When I test it with:</p>\n<pre><code>curl -v http://localhost:8000/\n</code></pre>\n<p>I get a response like this:</p>\n<pre><code>HTTP/1.0 200 OK\nContent-Length: 52\nContent-Type: application/json\nServer: BaseHTTP/0.6 Python/3.10.0\n</code></pre>\n<p>And the body is:</p>\n<pre><code>{&quot;message&quot;: &quot;Hello bro, this is your HTTP response&quot;}\n</code></pre>\n<p>I understand that:</p>\n<pre><code>self.send_response(200)\n</code></pre>\n<p>sets the HTTP status code,</p>\n<pre><code>self.send_header(...)\n</code></pre>\n<p>adds headers,</p>\n<p>and</p>\n<pre><code>self.wfile.write(response_body)\n</code></pre>\n<p>writes the response body.</p>\n<p>But I am confused about the exact order and internal flow.</p>\n<p>My questions are:</p>\n<ol>\n<li><p>Does <code>send_response()</code> immediately send data to the client, or does it store the status line temporarily?</p>\n</li>\n<li><p>Are headers actually sent only when <code>end_headers()</code> is called?</p>\n</li>\n<li><p>Why do we need to encode JSON using <code>.encode(&quot;utf-8&quot;)</code> before writing it?</p>\n</li>\n<li><p>Why does the response show <code>HTTP/1.0</code> even though I am using <code>curl</code> and the request says <code>GET / HTTP/1.1</code>?</p>\n</li>\n<li><p>Is this similar to what frameworks like FastAPI do internally, but with more abstraction?</p>\n</li>\n</ol>\n<p>I am trying to understand the low-level HTTP response flow before moving to FastAPI.</p>\n<p>What is the correct way to think about this process?</p>\n"}],"has_more":false,"quota_max":300,"quota_remaining":297}